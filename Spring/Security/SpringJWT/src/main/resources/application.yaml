spring:
  datasource:
    url: jdbc:mysql://localhost:3306/SpringSecurity_JWT?useSSL=false&allowPublicKeyRetrieval=true&characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: test1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create

#    jpa:
#      hibernate:
#        ddl-auto: create-
#      properties:
#        hibernate.format_sql: true
#        hibernate:
#      #        dialect: "org.hibernate.dialect.MySQL5Dialect"
#      show-sql: true
    properties:
      hibernate:
        format_sql: true
        show_sql: true