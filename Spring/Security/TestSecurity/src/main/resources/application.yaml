spring:
  datasource:
    url: jdbc:mysql://localhost:3306/SpringSecurity?useSSL=false&allowPublicKeyRetrieval=true&characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: test1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
  mustache:
    servlet:
      expose-request-attributes: true

server:
  servlet:
    session:
      timeout: 1800 #???